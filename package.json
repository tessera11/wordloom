{
  "name": "wordloom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WordLoom — a procedural word-connect PWA. Connect letters, weave crossword grids, discover bonus words.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
